'use strict';

global.tester('ui-modules/me-banner/components/me-banner-item', (expect, sinon, testFixtures) => {
    testFixtures();
    // testComponent({
    //     fixture: fixtures['single-footer-link']
    // }, () => {
    //     describe('when me-banner-item with single footer link is rendered', () => {
    //         it('footer section should have no multiple class', () => {
    //             expect(marko.component.getEl().querySelector('.banner-item__link')).to.be.not.null;
    //             expect(marko.component.getEl().querySelector('.banner-item__link').getAttribute('_sp')).to.be.not.null;
    //             expect(marko.component.getEl().querySelectorAll('.banner-item__footer a').length).to.equal(1);
    //             expect(marko.component.getEl().querySelector('.banner-item__link multiple')).to.be.null;
    //         });
    //     });
    // });

    // testComponent({
    //     fixture: fixtures['multi-footer-link']
    // }, () => {
    //     describe('when me-banner-item with multi footer links are rendered', () => {
    //         it('footer section should have multiple class', () => {
    //             expect(marko.component.getEl().querySelectorAll('.banner-item__footer a').length).to.equal(2);
    //         });
    //     });
    // });
});
